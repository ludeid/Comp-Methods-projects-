%%
close all

Y = importfile("mixture-observations.csv", [1, Inf]);

%histogram(Y)

theta_1 = 1;
n = length(Y);
N = 10^4;

theta= zeros(N,1);
theta(1) = theta_1;
for time = 2:N
    temp_prod = 1;
    for i = 1:n
        temp_prod = temp_prod*theta(time-1)*exp( -(Y(i)-1)^2/8 )
end